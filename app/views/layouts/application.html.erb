<!DOCTYPE html>
<html>
  <head>
    <title>Evoting</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "custom", "data-turbo-track": "reload" %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.2/font/bootstrap-icons.css">
    <link href="https://cdn.jsdelivr.net/npm/tom-select@2.0.3/dist/css/tom-select.css" rel="stylesheet">
    <%= javascript_importmap_tags %>
    <%= javascript_include_tag "https://www.gstatic.com/charts/loader.js" %>
  </head>
  <body>
     <%= render partial: "/layouts/navbar" if current_user %>
    
    <% #= puts "@@@@@@ #{params[:controller]}" %>
     <% # <%= render partial: '/layouts/navbar' unless params[:controller] == 'pages' || params[:controller] ==  'registrations' || params[:controller] ==  'elections' || params[:controller] == 'votes' || if user_signed_in? %>
     <div class=" <%= "container mt-3" unless params[:controller] == "pages" || params[:controller] == "elections" %> ">
      <div class="row d-flex justify-content-center">
        <div id="flash" style="width: auto" remote_modal>
          <%= render "layouts/partials/notification" %>
        </div>
      </div>
      <%= yield %>
      <%= turbo_frame_tag "remote_modal", target: "_top" %>
    </div>
  </body>
</html>
